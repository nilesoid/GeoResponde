# Base URL of the deployed Provider Gateway API.
# Local development can omit this (defaults to http://localhost:3001).
# In production set it to your deployed gateway, e.g.:
# VITE_API_URL=https://georesponde-gateway.vercel.app
VITE_API_URL=
